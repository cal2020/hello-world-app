{
  "name": "evidence-link-bench",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:public": "VITE_PUBLIC=1 vite build",
    "build:single": "node scripts/build-artifact.mjs",
    "preview": "vite preview",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "vite": "^7.1.7"
  }
}
